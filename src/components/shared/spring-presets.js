// Reusable spring configurations for GSAP
var SPRING = {
  bouncy: { ease: "elastic.out(1, 0.3)" },
  stiff: { ease: "back.out(1.7)" },
  gentle: { ease: "power3.out" },
  snappy: { ease: "back.out(1.2)" },
  wobbly: { ease: "elastic.out(0.8, 0.4)" },
};
