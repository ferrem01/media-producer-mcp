{
  "type": "social-proof",
  "label": "Social Proof",
  "category": "cta",
  "description": "A row or grid of proof items (logos, quotes, ratings, stats) that land one after another under a title.",
  "data": {
    "items": {
      "type": "array",
      "label": "Array of social proof items",
      "required": true,
      "items": {
        "type": "object",
        "properties": {
          "text": {
            "type": "string"
          },
          "subtext": {
            "type": "string"
          }
        },
        "required": [
          "text"
        ]
      }
    },
    "title": {
      "type": "string",
      "label": "Section title",
      "optional": true
    },
    "layout": {
      "type": "string",
      "label": "Layout mode",
      "optional": true,
      "enum": [
        "row",
        "grid"
      ],
      "default": "row"
    }
  }
}
