{
  "type": "stat-card",
  "label": "Stat Card",
  "category": "titles",
  "description": "Animated number counter with label. Number counts up from 0 to the target value. count:'roll' spins each digit into place like an odometer -- use it when the brief asks for rolling / ticking digits.",
  "data": {
    "value": {
      "type": "number",
      "label": "Value",
      "required": true
    },
    "prefix": {
      "type": "string",
      "label": "Prefix",
      "optional": true,
      "placeholder": "$"
    },
    "suffix": {
      "type": "string",
      "label": "Suffix",
      "optional": true,
      "placeholder": "%"
    },
    "label": {
      "type": "string",
      "label": "Label",
      "required": true
    },
    "decimals": {
      "type": "number",
      "label": "Decimal Places",
      "optional": true,
      "default": 0
    },
    "count": {
      "type": "string",
      "label": "count (default: the number counts up) | roll (odometer: each digit spins into place -- prices, metrics)",
      "optional": true,
      "enum": [
        "count",
        "roll"
      ]
    }
  }
}
